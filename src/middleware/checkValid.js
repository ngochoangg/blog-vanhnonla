// import mongoose from 'mongoose';

// export const checkObjectID = (req, res, next, val) => {
//   console.log('This func log an ObjectId if exist: ', val);
//   if (!mongoose.Types.ObjectId.isValid(val)) {
//     return res.status(400).json({
// status:'fail',
// message:'Invalid ID',
// });
//   }
//   next();
// };
